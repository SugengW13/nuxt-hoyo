<script lang="ts" setup>
// const { data, error, pending, refresh } = await useFetch('/api/hoyolab')

// if (error.value) throw createError(error.value)
</script>

<template>
  <div class="h-full flex flex-col items-center justify-center space-y-5">
    <nuxt-hoyo-logo />
  </div>
</template>
